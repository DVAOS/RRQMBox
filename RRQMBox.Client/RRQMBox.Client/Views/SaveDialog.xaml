<UserControl x:Class="RRQMBox.Client.Views.SaveDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="RRQM_Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RRQMBox.Client.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Height="200"
             Width="300"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Foreground="#FFF7F4F5"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Border Grid.RowSpan="3" Background="#1F1F1F" BorderBrush="#1B6DE0" BorderThickness="2" CornerRadius="10">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" Color="White" />
            </Border.Effect>
        </Border>
        <TextBlock Text="确定保存路径" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" />
        <TextBox x:Name="pathBox" Width="240" Grid.Row="1" Margin="5" HorizontalAlignment="Left"
                 Style="{DynamicResource inputBox}"
                 TextChanged="pathBox_TextChanged" />
        <controls:CorrugatedButton Content="..." Width="30" Grid.Row="1" Margin="5" HorizontalAlignment="Right"
                                   VerticalAlignment="Center" Click="CorrugatedButton_Click_1" CornerRadius="2" />
        <controls:CorrugatedButton Content="确定" Height="30" Width="100" Grid.Row="2" Click="CorrugatedButton_Click" />
    </Grid>
</UserControl>
